image: node:16.15.0-slim

stages:
  - testing

variables:
  POSTGRES_DB: braille
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_HOST: postgres

testing:
  services:
    - postgres:latest

  before_script:
    - yarn install
  stage: testing
  script:
    - echo "hi~"
